apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: a2w
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/kubeden/kubeden.git
    targetRevision: main
    path: k8s-cluster-configuration/applications/a2w/base
  destination:
    server: https://kubernetes.default.svc
    namespace: a2w
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
