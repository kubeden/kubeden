apiVersion: apps/v1
kind: Deployment
metadata:
  name: a2w
  namespace: a2w
  labels:
    app: a2w
spec:
  replicas: 1
  selector:
    matchLabels:
      app: a2w
  template:
    metadata:
      labels:
        app: a2w
    spec:
      containers:
      - name: a2w
        image: registry.k6nis.dev/a2w/website:latest
        ports:
        - containerPort: 3000
