apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: agentgrant-relay
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/kubeden/kubeden.git
    targetRevision: main
    path: k8s-cluster-configuration/applications/agentgrant-relay/base
  destination:
    server: https://kubernetes.default.svc
    namespace: agentgrant-relay
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
