apiVersion: apps/v1
kind: Deployment
metadata:
  name: ghostwrite
  namespace: ghostwrite
  labels:
    app: ghostwrite
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: ghostwrite
  template:
    metadata:
      labels:
        app: ghostwrite
    spec:
      containers:
        - name: ghostwrite
          image: registry.k6nis.dev/ghostwrite:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              name: http
          envFrom:
            - configMapRef:
                name: ghostwrite-config
            - secretRef:
                name: ghostwrite-secrets
          readinessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 20
            periodSeconds: 30
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
