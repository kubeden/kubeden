apiVersion: v1
kind: Service
metadata:
  name: innkeeper-landing
  namespace: innkeeper
spec:
  selector:
    app: innkeeper-landing
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
