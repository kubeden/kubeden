apiVersion: v1
kind: Service
metadata:
  name: innkeeper-gateway
  namespace: innkeeper
spec:
  selector:
    app: innkeeper-gateway
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
