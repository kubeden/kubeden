apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The page at `/`, served from the pinned a2wio/innkeeper-landing image.
# The route that sends `/` here is ../host.yml.
resources:
  - deployment.yml
  - service.yml
