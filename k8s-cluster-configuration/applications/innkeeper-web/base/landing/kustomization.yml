apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The page at `/`. Content is ConfigMaps applied from a2wio/innkeeper-landing;
# the route that sends `/` here is ../host.yml.
resources:
  - deployment.yml
  - service.yml
