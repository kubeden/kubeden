apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: justcodexit
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/kubeden/kubeden.git
    targetRevision: main
    path: k8s-cluster-configuration/applications/justcodexit/base
  destination:
    server: https://kubernetes.default.svc
    namespace: justcodexit
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
