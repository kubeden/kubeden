apiVersion: v1
kind: ServiceAccount
metadata:
  name: multirun-core
  namespace: multirun
---
# the runner's SA has no rbac at all: the runner never talks to the
# k8s api, and the cluster should hold it to that
apiVersion: v1
kind: ServiceAccount
metadata:
  name: multirun-runner
  namespace: multirun
