configs:
  cm:
    exec.enabled: true
    kustomize.buildOptions: "--enable-helm"
  params:
    server.insecure: true

applicationSet:
  enabled: true

server:
  service:
    type: ClusterIP
  ingress:
    enabled: false